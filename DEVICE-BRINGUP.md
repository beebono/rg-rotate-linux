# RG Rotate device bring-up status

Board: `ums512_1h10` (Unisoc T618 / UMS512, sharkl5pro). For build/flash/connect
recipes and repo layout see the [README](README.md).

## Status

**Mainline 7.1 boots a Debian 13 rootfs with a working USB serial console AND
a live panel.**

- Current dev iteration boots from **microSD** via U-Boot's extlinux scan
  (`mmc 1:1`, ahead of the eMMC slots) — see the README's "Clone"/build
  section for the `build_sdcard_debian.sh` flow. The busybox initramfs
  `/init` `switch_root`s into Debian 13 (trixie) on `/dev/mmcblk0p2`.
- eMMC (`boot_a` + `mmcblk3p74` userdata) remains the on-device/"installed"
  flow and recovery fallback — kernel `7.1.0-sprd-ums512+` boots from
  `boot_a` there, same `switch_root` flow onto Debian.
- Interactive autologin root shell over USB-C at host `/dev/ttyACM0` (CDC-ACM
  gadget), no UART hardware required.
- eMMC enumerates; both watchdogs are fed by `hw-watchdog.service`; backlight
  stays on through boot.

Current stage: userland hardware bring-up. The live serial console, on-disk
Debian, and on-screen console are the primary workflow.

## Bring-up checklist

- [x] Storage / rootfs
- [x] Display / DRM — cold kernel-native init works (fbcon + Debian login on panel), no U-Boot handoff needed — see [docs/DISPLAY-BRINGUP.md](docs/DISPLAY-BRINGUP.md)
- [x] Clocks/regulators cleanup — both `*_ignore_unused` cmdline flags dropped; eMMC/SD supplies wired in DTB
- [x] Power / PMIC / fuel gauge / charger — SC2730 PMIC + regulators healthy; `sc2730_fgu` reporting battery V/SoC/temp (`/sys/class/power_supply/sc27xx-fgu`); AW32257 charger now driven by mainline `bq2415x` (`/sys/class/power_supply/bq24158-0`, USB/online). Root cause of the long-dead i2c4 bus was a wrong AP-APB DT address (`apapb` identity `ranges;` vs offset child addrs → i2c4 at `0x00700000` DDR, not `0x70700000`); fixed in `ums512.dtsi`. See [docs/POWER-BRINGUP-NOTES.md](docs/POWER-BRINGUP-NOTES.md)
- NOTE: that same `ap-apb` `ranges` fix should also resolve **UART0 hanging when enabled** (uart0/uart1/i2c0-3/spi0-3 were all mis-addressed the same way; only eMMC/sdio worked because they used absolute addresses).
- [x] CPU freq / thermal — **cpufreq working, thermal sensors live, throttling wired.**
  - *Sensors:* all 3 `sprd,ums512-thermal` controllers (`ap_thm0/1/2`) probe, 12 `thermal_zone*` report sane calibrated temps (CPU clusters/cores, GPU). DT (nodes + efuse cal cells + zones) was already complete in `ums512.dtsi`; efuse shadow at reserved-mem `nvmem@800` is populated so `sen_delta_cal` calibration works.
  - *cpufreq:* register-level `sprd,sharkl5pro-cpudvfs` engine ported (4.14 vendor → 6.16) and **working on-device**: `policy0` = cpu0-5 (A55, 614MHz–1.82GHz), `policy6` = cpu6-7 (A75, 1.23–2.002GHz), schedutil scaling. `CONFIG_ARM_SPRD_CPUFREQ_HW=y`. The in-tree `sprd-cpufreq-v2` SMCCC-SIP path was a dead end here (stock firmware returns NOT_SUPPORTED for `SPRD_SIP_SVC_DVFS_*`).
  - *Thermal throttling — DONE.* The cpufreq driver sets `CPUFREQ_IS_COOLING_DEV`, so the core registers a cooling device per policy (`cpufreq-cpu0`, `cpufreq-cpu6`). The per-cluster `cooling-maps` reference the policy-leader CPUs (`&CPU0`/`&CPU6` — one cooling dev per policy, shared cluster clock), so the 70 °C passive trips now cap cluster frequency; 110 °C critical trip remains the shutdown backstop. (`No trip points found for thermal id=0` is benign — the trip-less `gpu`/`gpuank2` monitor-only zones.)
- [x] GPU — **Mali-G52 (Bifrost MP2) live under mainline panfrost (built-in).** Added `gpu@60000000` to `ums512.dtsi` (compatible `sprd,ums512-mali`/`arm,mali-bifrost`, IRQ 60 ×3, `gpu_clk` core/mem/bus, `&pmu UMS512_POWER_DOMAIN_GPU_TOP`, reset, `vddgpu`, OPP 384–850 MHz), enabled in `ums512-1h10.dts`, plus a `sprd,ums512-mali` match in `panfrost_drv.c`; `CONFIG_DRM_PANFROST=y`. Validated on-device: `mali-g52 id 0x7402`, `shader_present=0x3`, `/dev/dri/renderD128`; devfreq enumerates all 5 OPPs (`simple_ondemand`, auto-ramps off the 26 MHz boot default to 384 MHz); a direct uAPI smoke test (GET_PARAM + CREATE_BO → nonzero GPU VA + mmap readback) passes, confirming the GEM/MMU datapath. **Key fix:** a latent bug in `drivers/clk/sprd/ums512-clk.c` — `ums512_clk_probe` used `platform_get_drvdata()` (a `sprd_clk_drvdata *`) directly as the reset `regmap`, so the GPU's reset-deassert (the first ums512 node to request a reset) panicked with PC=`0x0` in `sprd_reset_deassert`; fixed to `data->regmap` (matches `ums9230-clk.c`) — candidate for upstreaming. Still untested: actual shader execution via `SUBMIT` (needs mesa/panfrost userspace, not yet installed — no network on device).
- [x] SD card — **working at UHS-I SDR104.** `&sdio0` in `ums512-1h10.dts` just needed `status = "okay"` (it inherited `disabled` from the dtsi; supplies `vddsdcore`/`vddsdio` + UHS PHY delays were already present). Stock detects cards via the controller's internal `sd-detect-*-syscon` registers, but mainline `sdhci-sprd` sets `SDHCI_QUIRK_BROKEN_CARD_DETECTION` and ignores them, and this board has no dedicated CD GPIO (infinix-x6816d used `eic_sync 19`), so added `broken-cd` to fall back to mmc-core polling. On-device: `mmc0` enumerates a 60 GiB SDXC card as SDR104, `/dev/mmcblk0p1` mounts and reads fine.
- [x] UART — **both UARTs enabled and probing clean.** `&uart0`/`&uart1` were `disabled`; flipping to `okay` no longer hangs (the AP-APB `ranges` fix cleared the old uart0 hang). Two more fixes were needed: (1) the dtsi node's bare `clocks = <&ext_26m>` matches none of sprd_serial's named lookups, and a non-console port treats a missing `enable` gate as fatal (`-ENOENT`) — wired the three named clocks per vendor `whale.dtsi`: `uart`=`<&ap_clk CLK_UART{0,1}>`, `source`=`<&ext_26m>`, `enable`=`<&apapb_gate CLK_UART{0,1}_EB>`; (2) `CONFIG_SERIAL_8250` was claiming `ttyS0..7` (same `ttyS` dev_name/major as sprd_serial → `duplicate filename '/class/tty/ttyS0'` + `Cannot register tty device`) — disabled `SERIAL_8250`/`SERIAL_8250_CONSOLE` (no 8250 HW on this SoC). On-device: `ttyS0`+`ttyS1` register, console on `ttyS1`. Remaining `request {TX,RX} DMA channel failed, ret = -19` is benign (no `dmas` wired → PIO fallback). **This unit has no known broken-out UART pads**, so this is "enabled + clean boot, no regression," not a verified serial link.
- [x] Audio — **speaker playback working** (real-time, clean) via the full DSP-mediated VBC path.
  - *Stock HW:* in-PMIC codec `sc2730`, DSP-mediated VBC (`vbc-v4-dsp.c`, IPC over `AGDSP_CH_VBC_CTL`), digital codec, MCDT (R1) + agcp DMA transport. **Speaker = codec headphone path → external Awinic AW87391 smart-amp** on i2c2 `0x58`. NB: the amp is driven **over I2C** (`anbernic_rgds_amp_enable()` in `aw87390.c`), **not** the GPIO-9 pulse mechanism — that (and `/sys/kernel/extpa/*`) is vestigial on this board. Speaker uses the codec **HP** mixers, not the SC2730 class-D SPK pin (`SPKL Gain`/`SPKL Mixer` stay off).
  - **Done:** AGDSP boots on mainline (ported `sprd-audcp-boot`, `sprd,ums512-audcp-boot`; stock `l_agdsp` firmware in the initramfs; both AGDSP power domains reach full wake-lock), the full DSP-mediated ASoC stack (VBC/MCDT/digital-codec/card + AW87391 amp) is wired to ums512, AP↔AGDSP IPC round-trips, and the speaker plays cleanly at real-time throughput.
  - *Mixer dependency:* playback needs an `asound.state` (DAC→HPL/HPR→HP path switches + **`DSP VBC Profile Select = 961216512`**, the load-bearing AGDSP VBC-profile selector — silent at its default). Recipe captured in `docs/stock_mixer_{idle,during}.txt`; `alsactl store` persists it. Driver seeds the HP/DAC analog gains on DAC power-up and enables the **AG_IIS0 → audio-top** route at codec probe (defaults off → otherwise silent).
  - *Robustness fixes (2026-07-10):* honour the agcp-ahb null-check in `arch_audio_iis_to_audio_top_enable` (kills an `alsactl`-at-boot NULL-deref panic); gate codec digital-register + AG_IIS0 writes on `agdsp_can_access()` and pin the AGDSP core awake (idle `amixer` reads no longer fatal-SError); **ums512 R1 mailbox binding** — the DT mislabelled it `ums9230` (R2), so the inbox ISR read the wrong FIFO bits and the IRQ auto-disabled ("nobody cared").
  - *Also ported, dormant — AP-side i2s0:* the vendor `i2s` CPU-DAI + `all-i2s` null-codec card, proven working end-to-end (real-time `ap_dma`, completion IRQ fires — no hrtimer needed). But IIS0 is the stock `i2s_bt_sco0` **Bluetooth-SCO** link, not the speaker, so the card ships **disabled**. Key unlock was the DMA **request-UID** fix (`dmas` cell is the UID now that `dma_slave_config.slave_id` was removed upstream: `ap_dma 4/3`, not 5/6).
- [x] Wi-Fi / Bluetooth — **Both DONE.** *Stock HW:* single **`unisoc,marlin3lite_sdio`** WCN combo (Marlin3 Lite / SC2355) on the `71400000.sdio` bus, fanning out to `sprd,sc2355-sdio-wifi` (wlan), `sprd,mtty` → `ttyBT` (BT), `sprd,gnss`, and `sprd,marlin3-fm`. GPIOs: enable 0x8f, reset 0x5f, wakeup-ap 0x21, irq 0x5e; supplies avdd12/avdd33/dcxo18 + clk_32k.
  - *Wi-Fi — working, line rate both directions.* A from-scratch mainline SC2355 SDIO driver (`drivers/net/wireless/unisoc/`, `sc23xx_wlan{,_sdio}`) on the BSP `sprdwcn` bus (sdiohal). Associates + WPA2 4-way + DHCP, holds a stable link, and over iperf3 does **~25 Mbit/s up / ~176 Mbit/s down at 0 retransmits**. Getting there took: the firmware power-save wake handshake on the SDIO data path, honoring the all-zero FLOWCON credit reset, the RX BA reorder rewrite (collect-then-deliver outside `sta_lock`, timer arming, timeout off-by-one), TX-credit **backpressure** (netif stop/wake + park-on-starvation instead of drop) with a HOST_IDLE-only-when-idle fix for the reconnect flap, and the **A-MSDU** `last_msdu_of_mpdu` bit fix that unlocked download throughput. No mainline marlin3 driver existed; vendor reference under `vendor/linux-kernel-5-4-ums512/.../wcn/wlan/wlan_combo/sc2355/`.
  - *Bluetooth — working, scan + pairing verified on device.* New `btsprdsdio` driver (`drivers/bluetooth/btsprd_hci.c` + `btsprd_ini.c`) registers a native `hci0` over the same `sprdwcn` SDIO bus, bound to `sprd,mtty` (BT_TX chn 3 / BT_RX chn 17, `start_marlin(MARLIN_BLUETOOTH)`). Forked from mainline `btsprdsipc` — kept its `hci_dev` setup, H4 RX reassembly and the Unisoc pskey/RF/enable vendor-command init sequence verbatim; only the transport was swapped from rpmsg/SIPC to the SDIO mchn bus. The pskey/RF blobs are parsed in-kernel from the vendor text INI (staged at `/lib/firmware/sprd/bt_configure_{pskey,rf}.ini`) for parity with the Wi-Fi config path, so no `libbt-vendor` HAL, `hciattach`, or offline packer is needed. Getting there took: the `local-bd-address`/`HCI_UNCONFIGURED` gate, clearing the LMP **Park** feature bit the SC2355 firmware advertises but rejects in a default link policy, and a **stable per-unit BD address derived from the SoC die UID** (efuse `uid-start`/`uid-end`). Scanning, discoverable, and pairing (Pixel + a BT DAC) all verified. Remaining is userspace-only: A2DP/HFP needs a media backend (PipeWire/WirePlumber). Scope/notes in [docs/BLUETOOTH-SCOPING.md](docs/BLUETOOTH-SCOPING.md); vendor reference under `vendor/linux-kernel-5-4-ums512/.../wcn/bluetooth/driver/tty-sdio/`.
- [x] Input — *working, all buttons + system keys evtest-confirmed.* Replaced the stock out-of-tree **`singleadc-joypad`** with plain mainline **`gpio-keys`** nodes in [ums512-rg-rotate.dts](src/linux-7-1-sprd/arch/arm64/boot/dts/sprd/ums512-rg-rotate.dts). This SKU is stickless, so the entire stock ADC stick path (`abs_*`, 6-way `amux`, `adc-power-ctl1-4`) is inert and **every real input is a discrete GPIO** — so gpio-keys *does* work here (buttons carry `BTN_*` gamepad codes directly; `joydev` binds → `/dev/input/jsX`). `gamepad_keys` covers face buttons, shoulders, Start/Select/Back/F9/F10, and the **D-pad remapped from stock's shared `ABS_HAT` hats to discrete `BTN_DPAD_*`** (gpio-keys' EV_ABS only fires on press, never recenters). Thumb-stick clicks dropped (no sticks). `volume_keys` (autorepeat) + `sys_keys` (Power, clean shutdown). Key catch: 8 buttons sit on dedicated `UMS512_GPIOnnn` pads (work as-is); the other 11 (+vol-up, +hall) are on RF/IIS/KEYIN/SPI **alternate-func pads** that needed **func4 muxing** (= the u-boot pinmap's `0x30`) + input-enable + pull-up via `&pinctrl` groups — the pad↔line map came from [gpio_pin.c](src/u-boot/board/spreadtrum/ums512_1h10/gpio_pin.c) `gpio_fun_pin[]`. The alternate **`sprd,spi-mcu-joy`** path is a different HW rev (RG Vita) — ignored.
- [x] Sensors — *working (Hall).* *Stock HW: Hall switch only* — no accel/gyro/mag/als/prox anywhere in the stock DT. Wired as **`EV_SW` / `SW_TABLET_MODE`** (screen rotates → persistent orientation *state*, not a keypress; gpio-keys reports level on every edge + initial state at probe) on **ap_gpio 54 (= `SPI2_DI`, func4-muxed)** per the real firmware `unpacked.dts` (`key-hall` gpio `0x36`). evtest-confirmed: toggles 1 on swivel-to-close, 0 on open. (An older decompiled overlay showing eic_debounce line 15 / `0x0f` was a mismatched variant — ignored.)
- [x] Touchscreen — **working, evtest-confirmed accurate touches.** *Stock HW:* generic **`goodix,gt9xx`** at i2c3 `0x14`, 720×720 (`0x2d0`), vcc gpio 0x10 / irq 0x90 / reset 0x91, embedded `cfg-group0` config blob — identified as a **GT911** (paired `lcd_gt911_mipi_ab021` panel name + 186-byte cfg-group0 matches the GT911-class config length). Ported to mainline `drivers/input/touchscreen/goodix.c` in [ums512-rg-rotate.dts](src/linux-7-1-sprd/arch/arm64/boot/dts/sprd/ums512-rg-rotate.dts): `compatible = "goodix,gt911"` on `&i2c3` (needed no explicit `pinctrl-0` — i2c0-i2c3 use matrix-routed pads already muxed at boot, unlike i2c4's shared SIM pins), `vcc-gpios` rewrapped as a `regulator-fixed` (`touch_vdd`, mainline wants `AVDD28-supply`/`VDDIO-supply`, not a raw GPIO toggle) since the mainline driver reads config back from the chip over I2C rather than consuming the vendor `cfg-group0` blob or any other `goodix,*` tuning prop. `CONFIG_TOUCHSCREEN_GOODIX=y` (not the unrelated `CONFIG_I2C_HID_OF_GOODIX`). **Key fix:** `irq-gpios`/`reset-gpios` must be `GPIO_ACTIVE_HIGH`, not the stock overlay's `ACTIVE_LOW` — `goodix_reset_no_int_sync()` writes literal 0/1 values it expects to map straight to the physical pin (drives INT high/low during reset to select the 0x14 vs 0x5D I2C address), so `ACTIVE_LOW` inverted that and caused a blanket -5/EIO on every transaction. Also needed `touchscreen-inverted-x`/`touchscreen-inverted-y` (panel mounted rotated relative to scanout — evtest showed (0,0) at the bottom-right before the fix). Default (non-swiveled) orientation matches panel scanout, so no further calibration mapping needed.
- [x] USB host / OTG — **automatic dual-role working.** Both roles switch on their own from Type-C CC detection via the sc2730 PMIC port manager (`sc27xx_pd`): plugging into a host selects device (CDC-ACM gadget console enumerates), plugging in a device selects host (confirmed enumerating an Avantree Leaf HID). Host-mode VBUS is sourced by the AW32257 (`bq2415x`) charger's OTG boost — there is no discrete 5V regulator — exposed as a `usb-otg-vbus` regulator the TCPM drives on host attach. The old userspace forced-peripheral oneshots are removed; the inert `linux,extcon-usb-gpio` node was dropped (nothing consumed it). Implementation + gadget-mode `-71` history in [docs/USB-OTG-HOST-CLEANUP.md](docs/USB-OTG-HOST-CLEANUP.md) and [docs/WHAT-HAS-BEEN-TRIED-USB.md](docs/WHAT-HAS-BEEN-TRIED-USB.md).
- [x] Status LED(s) — **working.** `&led1`/`&led2` overridden in `ums512-rg-rotate.dts` off the PMIC `led-controller@180`; defaults to green-on at boot, with `led1`/`led2` relabelled blue/yellow to match this SKU's actual silkscreen colors (stock's naming didn't match the hardware). Live color/state switching is left to userspace (`/sys/class/leds/*`) rather than encoding charge-state policy in the DT.
- [x] RTC — **working.** `sc2730-rtc` comes from `sc2730.dtsi` (pulled into the board DT by default, not disabled) and holds time across reboots — confirmed by testing (clock stays correct instead of resetting to kernel build/epoch time).
- [ ] Vibration / rumble — **untested.** `vibra` (`sprd,sc2730-vibrator`) is likewise enabled by default via `sc2730.dtsi`, so it isn't a DT-porting job — just needs on-device testing (does it actually buzz, and does a mainline driver bind to it) plus mapping into `ff-memless`/`/dev/input/eventX` force-feedback for RetroArch and friends.

## Polish-level targets
- [x] PM / Suspend-Resume — **working, deep suspend (`echo mem`) survives 5+ min and resumes cleanly.** Five independent blockers, fixed in order:
  1. **FGU aborts suspend (`-19`).** `sc27xx_fgu_suspend` → `sc27xx_fgu_get_status` returned `-ENODEV`: the fuel gauge's battery had no supplier providing `POWER_SUPPLY_PROP_STATUS`, and its hardcoded fallback list (`sc2731/2720/2721/2723_charger`) doesn't include this board's mainline `ti,bq24158` (bq2415x, psy name `bq24158-0`). **Fix:** added `power-supplies = <&charger>;` to `&sc2730_fgu` in [ums512-rg-rotate.dts](src/linux-7-1-sprd/arch/arm64/boot/dts/sprd/ums512-rg-rotate.dts) so `power_supply_get_property_from_supplier()` resolves STATUS from the bq2415x (which does expose it).
  2. **WCN wifi suspend times out (`-110`).** The wiphy's parent is a `platform_device` while sdiohal hangs off an `sdio_func`, so the two are unordered in the dpm list and cfg80211's `.suspend` tried to send the power-save command *after* sdiohal had quiesced the bus. **Fix:** moved the firmware suspend/resume handshake onto the sdiohal `power_notify` callback (runs while the bus is still up), wired onto the TX-CMD channel in [sdio.c](src/linux-7-1-sprd/drivers/net/wireless/unisoc/sdio.c); dropped the cfg80211 `.suspend/.resume`. Side benefit: engages `unisoc_wcn_slp`'s `power_notify` bool, fixing the `sdio power_notify is NULL` pub_int / wifi-powersave path.
  3. **cpufreq resume WARN.** `sprd-cpufreqhw` built `policy->cpus` from the online-dependent `topology_cluster_cpumask()`; during resume CPUs come up one at a time, so the big cluster's policy got `related_cpus = {6}` and CPU7 coming online tripped `WARN_ON(!cpumask_test_cpu(cpu, related_cpus))`. **Fix:** derive the sharing mask from the DT `cpufreq-data-v1` phandle (online-independent) via new `sprd_cpufreq_get_sharing_cpus()` in [sprd-cpufreqhw.c](src/linux-7-1-sprd/drivers/cpufreq/sprd-cpufreqhw.c).
  4. **musb USB controller aborts suspend instantly.** musb armed its IRQ as a system wake source with a bare `enable_irq_wake()` at init (upstream's own `/* FIXME this handles wakeup irqs wrong */`) and never consulted `device_may_wakeup()`, so the `power/wakeup` sysfs knob was a no-op and routine USB traffic (~10/s while cabled for serial) woke the system immediately. **Fix in [musb_core.c](src/linux-7-1-sprd/drivers/usb/musb/musb_core.c):** advertise wakeup capability but arm/disarm the wake IRQ in suspend/resume gated on `device_may_wakeup()`, defaulting disabled (USB host core still re-enables at hcd setup, so the now-functional sysfs knob or a udev rule controls it).
  5. **Watchdog resets mid-sleep.** A custom `hw-watchdog.service` feeds both `/dev/watchdog0` (AP `sprd_wdt`) and `/dev/watchdog1` (PMIC `sc27xx_wdt`) from userspace; frozen during suspend, it stops petting. `sprd_wdt` already stops on suspend but `sc27xx_wdt` had **no PM ops** → free-ran and reset the SoC on sleeps longer than its timeout (short test-suspends survived, long ones didn't). **Fix:** restored the mainline suspend/resume PM ops (stop-on-suspend / restart-on-resume) to [sc27xx_wdt.c](src/linux-7-1-sprd/drivers/watchdog/sc27xx_wdt.c).
- [x] CPU governor / perf profile for gaming — **working.** All power profiles enabled, performance mode correctly pins CPU clocks and is settable via sysfs.

## U-Boot 2nd-pass targets
- [x] Actual Panel/Bootlogo — **working.** Full logo and lcd console support confirmed.
- [x] Dual-boot Android-from-eMMC/Linux-from-microSD — **working.** Custom U-Boot can trampoline properly into stock U-Boot, but this is handled by the SPL now.
- [x] Charge-while-Off Handling - **working.** Displays percentage as text and detects whether powered on by USB plug or power button. Can also hold power while charging to finish boot.

## The Big One
- [ ] Full OS of some kind. (Probably ROCKNIX)

## "If there's nothing else to do" target
- [ ] DDR devfreq — memory clock is presumably pinned at its boot frequency rather than scaling with load; the one confirmed gap outside PM. Needs scoping (does ums512 have a DDR devfreq/DFS driver upstream or in the vendor tree) and then power/perf tuning for sustained gaming loads vs idle.

Cross-reference `device/stock/dtb_stock.dts` whenever a node, supply, syscon, or
GPIO detail is unclear in mainline.

## Vendor driver source map (`vendor/linux-kernel-5-4-ums512/kernel_modules/`)

This is the **generic UMS512 Android BSP**, not a per-SKU tree — it carries
drivers for many sibling devices, so presence of a driver here does *not* mean
the part is on *this* board. Mapped against our stock-DT hardware:

- **Wi-Fi** → `kernel5.4/wcn/wlan/wlan_combo/**sc2355**/` is ours (matches overlay
  `sprd,sc2355-sdio-wifi`). The `merlion/` and `sc2332/` siblings in the same dir
  are **different chips — do not port those**. SDIO transport.
- **Bluetooth** → `kernel5.4/wcn/bluetooth/driver/**tty-sdio**/` (Marlin3 over
  SDIO, surfaces as `sprd,mtty` → `ttyBT`). `tty-pcie`/`tty-sipc*` are other transports.
- **FM** → `kernel5.4/wcn/fm/driver/**fm_sdio**/`. GNSS shares the marlin3 stack.
- **Audio** → `kernel5.4/audio_driver/sprd/` is the ASoC side (`codec`/`dai`/
  `machine`/`platform`); `kernel5.4/audio_driver/sprd_audio/` is the proprietary
  **AGDSP/SIPC** half (`agdsp_access`, `audiosipc`, `mcdt`, `audio_pipe`, …). The
  external `aw87xxx` speaker PA is wired *inside* the sprd ASoC machine driver
  (`sprd/machine/sprd_card/sprd-asoc-card-utils-hook*.c`), not as a standalone module.

**NOT in this tree** (Anbernic additions, sourced elsewhere):
- **Goodix `gt9xx` touchscreen** — no goodix code here at all; use mainline
  `goodix`/`Goodix-TS` (or the upstream out-of-tree gt9xx).
- **`singleadc-joypad` / `sprd,spi-mcu-joy` gamepad** — absent; the singleadc
  driver lives in handheld-distro trees (ROCKNIX/JELOS/Knulli), not the SPRD BSP.
- **`aw32257`/`bq2415x` charger** — absent (already solved on mainline `bq2415x`).

`kernel5.4/input/misc/` holds a large **generic sensor zoo** (`bma253`, `bmi160`,
`kionix`, `lis2dh`, `mc34xx`, `mir3da`, `stk8baxx`, `akm099xx`/`afx133` mag,
`ltr_558als`/`tcs3430` als, `vl53L0/L1` ToF) — none correspond to this board (stock
DT declares **Hall only**); useful only as a driver pick *if* a sensor ever turns up.
Likewise `input/touchscreen/` (focaltech/novatek/synaptics/…) does **not** include goodix.

## Subsystem detail

- **Display / DRM** — [docs/DISPLAY-BRINGUP.md](docs/DISPLAY-BRINGUP.md) (working recipe, panel data source, live-debug notes)
- **Power / PMIC / charger** — [docs/POWER-BRINGUP-NOTES.md](docs/POWER-BRINGUP-NOTES.md).
- **USB gadget / OTG** — [docs/WHAT-HAS-BEEN-TRIED-USB.md](docs/WHAT-HAS-BEEN-TRIED-USB.md).
